#include <main.h>
#include <_putchar.c>

/**
 * Checks for upper case character
 * _isupper returns 1 if true
 * c is the character variable
 */

int _isupper(int c);

int main(void)
{
	char c;

	c = 'A'

	if (_isupper(c))
		return (1);
	return (0);
}
